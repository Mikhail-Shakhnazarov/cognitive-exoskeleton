# TODO

- Operationalize patch workflow: when a `v1_patch_*.md` is executed, automatically update its frontmatter `status` (e.g., `draft` -> `applied`) and append a short `## Status` record of what changed + verification performed.
